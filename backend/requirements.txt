# Marketing Command Center — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.111.0          # Core API framework
uvicorn[standard]==0.29.0 # ASGI server with hot-reload support

# ── Data validation & serialisation ─────────────────────────────────────────
pydantic==2.7.1           # Request/response model validation (ships with FastAPI)

# ── Numerical computation ────────────────────────────────────────────────────
numpy==1.26.4             # Used in forecasting calculations (break-even, projections)

# ── Dev / testing (optional but recommended) ────────────────────────────────
httpx==0.27.0             # Async test client for FastAPI TestClient
pytest==8.2.0             # Test runner
pytest-asyncio==0.23.6    # Async test support